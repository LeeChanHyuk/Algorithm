#include <iostream>
#include <unordered_map>
#include "node.h"


Node* a = new Node();
using namespace std;

Node* head = new Node();

int main(void)
{
	cout << getNode(1) << head->start_col;
	return 0;
}